/// Customer dashboard as client components start from here
/// This is from where and within, we can call SessionProvider()
/// to validate and authenticate nextauth session, session that started
/// when user signed the msg

export default function CustomerDashboard({}) {
    return (<></>);
}