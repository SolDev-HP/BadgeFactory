/// Client side components can live here and within, 
/// SessionProvider in nextauth should be in client components only
/// So these will be entry points for that, EntityDashboard, CustomerDashboard

export default function EntityDashboard({}) {
    return (<></>);
}