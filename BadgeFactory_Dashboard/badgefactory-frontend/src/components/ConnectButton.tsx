// Basic wallet connect button
// For now we need a way for anyone to connect to badgefactory, 
// once that happens, we hit the subscribe route, where user can sign a message and subscribe 
// as entity (someone who wants to use badgefactory loyalty management system) or as customer
// (someone who interacts with loyalty systems and campaigns deployed by entities)
// Dashboard - should be 2 routes, customer-dashboard route, entity-dashboard route

export default function ConnectButton() {
    return <w3m-button />
}